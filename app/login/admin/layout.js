export const metadata = {
  title: "Admin Login — Ganesh English Secondary School",
  description: "Secure admin portal for Ganesh English Secondary School staff.",
};

export default function AdminLoginLayout({ children }) {
  return <>{children}</>;
}